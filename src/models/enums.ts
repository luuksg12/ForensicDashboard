export enum EvidenceType {
    Gunpowder = 0,
    Sperm = 1, 
    Saliva = 2, 
    Blood = 3,
    Fibers = 4,
    Fingerprint = 5,
}

export enum ActionType {
    Found = 0, 
    Dropped= 1,
    PickedUp = 2
}

export enum LightType{
    White = 0,
    Violet = 1,
    Blue = 2,
    BlueGreen = 3,
    Green = 4
}

export enum FilterType{
    'Base' = 0,
    '435nm' = 1,
    '476nm' = 2,
    '529nm' = 3,
    '549nm' = 4,
}
