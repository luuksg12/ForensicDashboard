import { Button } from "@mui/material";
import React from "react";
import { Http, Method } from './../../helper/Http'
import { HOST } from "../../Constants";
function DeleteBadge(props: { sessionId: string }) {
    return (
        <div>
            
        </div>
    );
} export default DeleteBadge