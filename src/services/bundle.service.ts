import { WebSocketService } from "./websocket.service";

export class BundelService{
    private tester = 12

    // public socketConnection = new WebSocketService()

    constructor(){
        console.log(this.tester)
    }
}